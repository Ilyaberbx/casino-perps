/**
 * Dark-module fill. Hard navy (`--background`'s dark value) on a white frame —
 * QR scannability needs maximum contrast, so this is a literal rather than a
 * theme token (the surrounding frame is always white regardless of theme).
 */
export const QR_DARK_FILL = '#0a202f'
