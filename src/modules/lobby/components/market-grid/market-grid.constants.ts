/** Placeholder cards shown while the venue universe loads. One grid's worth —
 *  matches HOT_MARKET_LIMIT so the skeleton fills the same shape the real view will. */
export const GRID_SKELETON_COUNT = 12

export const GRID_SKELETON_KEYS = Array.from({ length: GRID_SKELETON_COUNT }, (_, i) => i)
