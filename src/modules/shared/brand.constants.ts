/**
 * The casino's product name. Ships as `YEET` (PRD 0008 D10). This is the single
 * source of truth for the brand name — the Wordmark renders it typographically in
 * the display face; nothing hardcodes the string. Renaming the product is a
 * one-line change here (PRD §13 R2, the trademark mitigation on record).
 */
export const BRAND_NAME = 'YEET'
