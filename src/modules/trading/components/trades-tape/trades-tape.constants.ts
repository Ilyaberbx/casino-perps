/** Skeleton rows shown while the tape loads — sized to roughly fill the scrollable body before trades arrive. */
export const TRADES_SKELETON_ROWS = 18

/** Empty-state copy once the trades snapshot has arrived with no recent trades. See ADR-0030. */
export const NO_TRADES_MESSAGE = 'No recent trades'
