/**
 * How long the exiting skeleton stays mounted while it crossfades out. MUST stay
 * in sync with the `loading-reveal-out` animation duration in
 * `loading-reveal.module.css` — the timer unmounts the skeleton once its fade
 * completes.
 */
export const LOADING_REVEAL_EXIT_MS = 420
